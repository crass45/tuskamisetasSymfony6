<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"
        xmlns:xhtml="http://www.w3.org/1999/xhtml">

    {% for url in urls %}
        <url>
            <loc>{{ url.loc }}</loc>
            <changefreq>{{ url.changefreq }}</changefreq>
            <priority>{{ url.priority }}</priority>

            {# Enlaces alternativos de idioma #}
            {% for link in url.links %}
                <xhtml:link rel="alternate" hreflang="{{ link.lang }}" href="{{ link.href }}" />
            {% endfor %}

            {# Imágenes #}
            {% for image in url.images %}
                <image:image>
                    <image:loc>{{ image.loc }}</image:loc>
                    {% if image.title is defined %}<image:title>{{ image.title }}</image:title>{% endif %}
                    {% if image.caption is defined %}<image:caption>{{ image.caption }}</image:caption>{% endif %}
                </image:image>
            {% endfor %}
        </url>
    {% endfor %}
</urlset>