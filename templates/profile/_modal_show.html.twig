{# templates/profile/_modal_show.html.twig #}
{# Este fichero contiene el HTML para el modal cuando el usuario está logueado. #}
{# NO debe heredar de base.html.twig para evitar bucles de renderizado. #}

<div class="sonata-user-show row row-fluid">
    <div class="col-lg-12">
        <h2 style="margin-top: 0;">Hola, {{ app.user.username }}</h2>
        <p>
            Bienvenido de nuevo a TusKamisetas.com. Desde aquí tienes acceso a tu perfil de usuario, donde podrás modificar tus datos y ver el historial de tus pedidos.
        </p>

        {# MIGRACIÓN NOTA: Asegúrate de que estas rutas ('app_profile_edit', 'app_profile_orders') existen o las crearemos en los siguientes pasos. #}
        <a href="{{ path("app_profile_edit", {'_locale': app.request.locale}) }}">
            <button class="button" style="width: 100%; height: 40px; margin-top: 20px; display: flex; align-items: center; justify-content: center;">Mi Perfil</button>
        </a>

        <a href="{{ path("app_profile_orders", {'_locale': app.request.locale}) }}">
            <button class="button" style="width: 100%; height: 40px; margin-top: 20px; display: flex; align-items: center; justify-content: center;">Mis Pedidos</button>
        </a>

        <a href="{{ path('app_logout')}}">
            <button class="button" style="width: 100%; height: 40px; margin-top: 20px; background-color: #d9534f; display: flex; align-items: center; justify-content: center;">Cerrar Sesión</button>
        </a>
    </div>
</div>

